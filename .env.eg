# Hugging Face API Configuration
# Optional: For higher rate limits, sign up at https://huggingface.co/
# and get your API key from https://huggingface.co/settings/tokens

VITE_HUGGINGFACE_API_KEY=""

# Note: The app will work without an API key using the free tier,
# but with lower rate limits. For production use, it's recommended
# to obtain and use an API key.
